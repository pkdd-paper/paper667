# Add any project specific configuration here.

add.config(
  pbp_folder = "data/pbp",
  seasons = c(2017),
  deltas = c(2, 4, 6),
  results_folder = "data/res",
  clean_global_vars = TRUE,
  matching_seed = 222
)
